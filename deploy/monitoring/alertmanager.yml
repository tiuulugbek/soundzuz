route:
  receiver: default
  group_by: [alertname]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
receivers:
  - name: default
    webhook_configs:
      - url: ${ALERT_WEBHOOK_URL:-http://127.0.0.1:65535/disabled}
        send_resolved: true
