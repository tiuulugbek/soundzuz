{
  "name": "@soundz/worker",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/main.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@soundz/config": "workspace:*",
    "@soundz/database": "workspace:*",
    "bullmq": "^5.80.9",
    "dotenv": "^17.2.1",
    "ioredis": "5.7.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.15",
    "tsx": "^4.22.5",
    "typescript": "^5.9.2"
  }
}
