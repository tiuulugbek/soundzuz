import type { MetadataRoute } from "next";
const API_URL=process.env.NEXT_PUBLIC_API_URL??"http://localhost:4000/v1";
const SITE_URL=process.env.NEXT_PUBLIC_SITE_URL??"https://soundz.uz";
export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base:MetadataRoute.Sitemap=[{url:SITE_URL,changeFrequency:"weekly",priority:1},{url:`${SITE_URL}/eshitish-moslamalari`,changeFrequency:"daily",priority:.9},{url:`${SITE_URL}/foydali-malumotlar`,changeFrequency:"daily",priority:.8}];try{const [pr,ar]=await Promise.all([fetch(`${API_URL}/catalog/products`,{next:{revalidate:3600}}),fetch(`${API_URL}/content/articles?locale=uz&limit=100`,{next:{revalidate:3600}})]);const products=pr.ok?await pr.json():[];const articles=ar.ok?await ar.json():[];return [...base,...products.map((p:any)=>({url:`${SITE_URL}/eshitish-moslamalari/${p.slug}`,changeFrequency:"weekly" as const,priority:.8})),...articles.map((a:any)=>({url:`${SITE_URL}/maqolalar/${a.slug}`,lastModified:a.publishedAt?new Date(a.publishedAt):undefined,changeFrequency:"monthly" as const,priority:.7}))]}catch{return base}}
